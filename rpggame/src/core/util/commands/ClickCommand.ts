import { ICommand } from "../ICommand";

/**
 * Define the click behavior of an UI element.
 * @implements ICommand
 */
export class ClickCommand implements ICommand{
   
    constructor(){
        
    }

    execute(): void {
        
    }

}